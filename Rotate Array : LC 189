class Solution {
public:
    void rotate(vector<int>& nums, int k) {
        int n=nums.size();
        k%=n;
        for(int i=0;i<abs(n-k);i++){
            int l=nums[0];
            nums.erase(nums.begin());
            nums.push_back(l);
        }
    }
};
